language: cpp
compiler:
  - clang
  - gcc
branches:
  only:
    - master
    - develop
addons:
  apt:
    packages:
    - cmake
    - libboost-filesystem-dev
    - libboost-dev
    - libfreetype6-dev
    - libsdl2-dev
before_script:
  - mkdir build
  - cd build
  - cmake ..
script: make
